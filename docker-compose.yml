services:
  web:
    build: .
    container_name: pediatric_clinic
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
      - .env.docker-runtime
    environment:
      FLASK_ENV: production
