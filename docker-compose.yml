services:
  web:
    build: .
    container_name: claid
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
      - .env.docker-runtime
    environment:
      FLASK_ENV: production
    volumes:
      - uploads_data:/app/app/static/uploads

volumes:
  uploads_data:
