# Production overrides for VPS — use with:
# docker compose -f docker-compose.yml -f docker/docker-compose.prod.yml up -d --build

services:
  db:
    ports: []

  web:
    ports:
      - "127.0.0.1:${APP_PORT:-5052}:5052"
